% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_anomaly_score_integrity.R
\name{compute_score_integrity}
\alias{compute_score_integrity}
\title{Compute score integrity checks: invalid scores and level mismatches}
\usage{
compute_score_integrity(student_results_long, assessment_spec, min_n = 10)
}
\arguments{
\item{student_results_long}{A long-format student results data frame
(see data contract). Must contain \code{STUDENT_ID}, \code{YEAR}, \code{SUBJECT},
\code{GRADE}, \code{SCALE_SCORE}, and \code{ACHIEVEMENT_LEVEL}.}

\item{assessment_spec}{Named list describing the assessment program.}

\item{min_n}{Integer. Groups with fewer than \code{min_n} non-missing scores
have \code{pct_invalid} and \code{pct_mismatch} set to \code{NA}. Default \code{10}.}
}
\value{
A named list with three elements:
\itemize{
\item \verb{$summary} -- one row per \code{YEAR} x \code{SUBJECT} x \code{GRADE} with columns
\code{n_total}, \code{n_below_loss}, \code{n_above_hoss}, \code{n_invalid}, \code{pct_invalid},
\code{n_mismatch}, \code{pct_mismatch}, \code{n_unknown_level}, \code{has_any_issue}.
\item \verb{$invalid_records} -- individual rows with invalid scores; columns
\code{STUDENT_ID}, \code{YEAR}, \code{SUBJECT}, \code{GRADE}, \code{SCALE_SCORE}, \code{LOSS},
\code{HOSS}, \code{direction}. Empty data frame if none found.
\item \verb{$mismatch_records} -- individual rows with level mismatches (valid
scores only); columns \code{STUDENT_ID}, \code{YEAR}, \code{SUBJECT}, \code{GRADE},
\code{SCALE_SCORE}, \code{ACHIEVEMENT_LEVEL}, \code{EXPECTED_LEVEL}. Empty data frame
if none found.
}
}
\description{
Runs two checks against \code{assessment_spec}:
}
\details{
\strong{Check 1 -- Invalid scores}: flags any \code{SCALE_SCORE} below \code{LOSS} or above
\code{HOSS} for its subject/grade combination.

\strong{Check 2 -- Level mismatches}: for scores that passed Check 1, derives the
expected achievement level from the \code{cuts} vector
(\code{sum(score > cuts) + 1} gives the level index) and compares it to the
recorded \code{ACHIEVEMENT_LEVEL}. Rows where the recorded level is not in
\code{assessment_spec$achievement_levels$labels} are excluded from the mismatch
count and reported separately in the narrative.
}
\examples{
\dontrun{
source(system.file("specs/00_DEMO_Assessment_Spec.R", package = "ReportR"))
result <- compute_score_integrity(demo_data, assessment_spec)
result$summary
}
}
