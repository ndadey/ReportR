% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_anomaly_distributions.R
\name{plot_distributions}
\alias{plot_distributions}
\title{Plot distribution shape diagnostics}
\usage{
plot_distributions(distribution_table, student_results_long, assessment_spec)
}
\arguments{
\item{distribution_table}{Data frame returned by \code{\link[=compute_distributions]{compute_distributions()}}.}

\item{student_results_long}{A long-format student results data frame (used
to draw kernel density plots for flagged cells).}

\item{assessment_spec}{Named list describing the assessment program (used for
subject labels and LOSS/HOSS boundaries).}
}
\value{
A named list with:
\itemize{
\item \verb{$flag_summary} — bar chart of flagged cell counts by subject and flag type;
\code{NULL} if no cells are flagged
\item \verb{$shape_heatmap} — heatmap of \code{bimodality_coefficient} by grade × year,
faceted by subject
\item \verb{$density_flagged} — named list of kernel density plots for flagged cells
}
}
\description{
Returns a named list of three plots summarising the distribution shape
screening results.
}
\examples{
\dontrun{
tbl <- compute_distributions(demo_data, assessment_spec)
plots <- plot_distributions(tbl, demo_data, assessment_spec)
plots$shape_heatmap
}
}
