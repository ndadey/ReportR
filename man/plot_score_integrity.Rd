% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_anomaly_score_integrity.R
\name{plot_score_integrity}
\alias{plot_score_integrity}
\title{Plot score integrity results}
\usage{
plot_score_integrity(score_integrity_result)
}
\arguments{
\item{score_integrity_result}{Named list returned by
\code{\link[=compute_score_integrity]{compute_score_integrity()}}.}
}
\value{
A named list with:
\itemize{
\item \verb{$invalid} -- bar chart of \code{n_invalid} by subject and grade, faceted
by year. \code{NULL} (with message) if no invalid scores exist.
\item \verb{$mismatch} -- heatmap of \code{pct_mismatch} with year on x-axis, grade on
y-axis, faceted by subject. Grey cells = no mismatches. \code{NULL} (with
message) if no mismatches exist.
}
}
\description{
Produces two diagnostic plots from the output of \code{\link[=compute_score_integrity]{compute_score_integrity()}}:
a bar chart of invalid score counts and a heatmap of mismatch rates.
}
\examples{
\dontrun{
result <- compute_score_integrity(demo_data, assessment_spec)
plots  <- plot_score_integrity(result)
plots$mismatch
}
}
