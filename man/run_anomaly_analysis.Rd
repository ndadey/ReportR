% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_anomaly_analysis.R
\name{run_anomaly_analysis}
\alias{run_anomaly_analysis}
\title{Run all Line 4 score quality analyses}
\usage{
run_anomaly_analysis(
  student_results_long,
  assessment_spec,
  report_years = NULL,
  min_n = 10,
  point_adjust = 5,
  mild_thresh = 5,
  moderate_thresh = 10,
  severe_thresh = 15
)
}
\arguments{
\item{student_results_long}{A long-format student results data frame
(see data contract). Should already be processed by
\code{\link[=standardize_student_results]{standardize_student_results()}}.}

\item{assessment_spec}{Named list describing the assessment program
(see assessment_spec).}

\item{report_years}{Integer vector. Years to include. If \code{NULL} (default)
all years present in the data are used.}

\item{min_n}{Integer. Minimum group size for small-n suppression. Default
\code{10}.}

\item{point_adjust}{Integer. Scale-score points from LOSS/HOSS boundary
that defines "near". Default \code{5}.}

\item{mild_thresh}{Numeric. Percent threshold for Mild HOSS/LOSS flag.
Default \code{5}.}

\item{moderate_thresh}{Numeric. Percent threshold for Moderate flag.
Default \code{10}.}

\item{severe_thresh}{Numeric. Percent threshold for Severe flag.
Default \code{15}.}
}
\value{
A named list \code{anomaly_results} with sub-lists:
\itemize{
\item \verb{$loss_hoss} — output of \code{\link[=run_loss_hoss]{run_loss_hoss()}}
\item \verb{$score_integrity} — output of \code{\link[=run_score_integrity]{run_score_integrity()}}
\item \verb{$outliers} — output of \code{\link[=run_outliers]{run_outliers()}} (stub)
\item \verb{$correlations} — output of \code{\link[=run_correlations]{run_correlations()}} (stub)
\item \verb{$distributions} — output of \code{\link[=run_distributions]{run_distributions()}} (stub)
}
}
\description{
Orchestrates the five score quality sub-analyses (HOSS/LOSS pileups, score
integrity, year-to-year outliers, year-to-year correlations, and
distribution diagnostics) and returns a single named results list. All
subsequent report chunks should read from the returned object rather than
re-running analysis.
}
\examples{
\dontrun{
source(system.file("specs/00_DEMO_Assessment_Spec.R", package = "ReportR"))
results <- run_anomaly_analysis(
  student_results_long = demo_data,
  assessment_spec      = assessment_spec,
  report_years         = 2016:2019,
  min_n                = 10
)
}
}
