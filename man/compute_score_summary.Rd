% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_trend_overall.R
\name{compute_score_summary}
\alias{compute_score_summary}
\title{Compute scale score distributional summaries by year, subject, and grade}
\usage{
compute_score_summary(
  student_results_long,
  assessment_spec,
  probs = c(0.05, 0.25, 0.5, 0.75, 0.95),
  by_grade = TRUE,
  min_n = 10
)
}
\arguments{
\item{student_results_long}{A long-format student results data frame
(see data contract). Must contain \code{YEAR}, \code{SUBJECT}, \code{SCALE_SCORE}, and
\code{GRADE} when \code{by_grade = TRUE}.}

\item{assessment_spec}{Named list describing the assessment program.
Used to validate that subjects in the data match the spec.}

\item{probs}{Numeric vector of percentiles in [0, 1]. Default
\code{c(0.05, 0.25, 0.50, 0.75, 0.95)}.}

\item{by_grade}{Logical. If \code{TRUE} (default) summaries are computed per
\verb{YEAR × GRADE × SUBJECT}. If \code{FALSE}, \code{GRADE} is ignored and summaries
are per \verb{YEAR × SUBJECT}.}

\item{min_n}{Integer. Groups with fewer than \code{min_n} non-missing scores
have their statistics set to \code{NA}. Default \code{10}.}
}
\value{
A named list with four elements:
\itemize{
\item \verb{$ScaleScore_Stats_Long} — long-format data frame: grouping columns plus
\code{metric} and \code{value}. Metrics: \code{mean}, \code{sd}, \code{p05}, \code{p25}, \code{p50},
\code{p75}, \code{p95} (or as determined by \code{probs}).
\item \verb{$ScaleScore_Stats_Wide} — wide-format data frame: one row per group,
one column per metric.
\item \verb{$ScaleScore_Stats_Report} — report-ready wide table: one row per grade,
with year-grouped column blocks \verb{<YEAR>_Mean}, \verb{<YEAR>_SD}, \verb{<YEAR>_N}.
\item \verb{$ScaleScore_Counts} — data frame with \code{n_total}, \code{n_nonmissing},
\code{n_missing}, and \code{pct_missing} per group.
}
}
\description{
Produces long- and wide-format summary tables of student scale score
distributions, a counts table, and a report-ready wide table with grades as
rows and year-grouped column blocks (Mean / SD / N). Designed as the primary
data source for all Line 1 score trend reporting.
}
\examples{
\dontrun{
source(system.file("specs/00_DEMO_Assessment_Spec.R", package = "ReportR"))
out <- compute_score_summary(demo_data, assessment_spec)
head(out$ScaleScore_Stats_Wide)
}
}
