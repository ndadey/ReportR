% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_anomaly_loss_hoss.R
\name{compute_loss_hoss_table}
\alias{compute_loss_hoss_table}
\title{Compute HOSS/LOSS pileup rates by year, subject, and grade}
\usage{
compute_loss_hoss_table(
  student_results_long,
  assessment_spec,
  point_adjust = 5,
  min_n = 10,
  mild_thresh = 5,
  moderate_thresh = 10,
  severe_thresh = 15
)
}
\arguments{
\item{student_results_long}{A long-format student results data frame
(see data contract). Must contain \code{YEAR}, \code{SUBJECT}, \code{GRADE}, and
\code{SCALE_SCORE}.}

\item{assessment_spec}{Named list describing the assessment program
(see assessment_spec). Must contain \verb{$subjects} and \verb{$scale_scores}.}

\item{point_adjust}{Integer. Number of scale-score points within the
boundary that counts as "near" LOSS or HOSS. Default \code{5}.}

\item{min_n}{Integer. Minimum group size; cells with fewer students have
\code{pct_near_loss} and \code{pct_near_hoss} set to \code{NA}. Default \code{10}.}

\item{mild_thresh}{Numeric. Percentage threshold for a Mild flag. Default \code{5}.}

\item{moderate_thresh}{Numeric. Percentage threshold for a Moderate flag.
Default \code{10}.}

\item{severe_thresh}{Numeric. Percentage threshold for a Severe flag.
Default \code{15}.}
}
\value{
A data frame with one row per year × subject × grade combination,
columns: \code{YEAR}, \code{SUBJECT}, \code{GRADE}, \code{LOSS}, \code{HOSS}, \code{n},
\code{n_near_loss}, \code{n_near_hoss}, \code{pct_near_loss}, \code{pct_near_hoss},
\code{small_n}, \code{hoss_flag}, \code{loss_flag}, \code{pct_near_hoss_change},
\code{pct_near_loss_change}.
}
\description{
Joins student-level data to the LOSS/HOSS boundaries from \code{assessment_spec},
flags students within \code{point_adjust} points of either boundary, aggregates
to year × subject × grade, applies severity flags, and suppresses cells
with fewer than \code{min_n} students.
}
\examples{
\dontrun{
source(system.file("specs/00_DEMO_Assessment_Spec.R", package = "ReportR"))
tbl <- compute_loss_hoss_table(demo_data, assessment_spec)
}
}
